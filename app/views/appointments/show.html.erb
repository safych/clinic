<div style="margin-top: -3%; margin-left: 1%">
  <p style="color: green"><%= notice %></p>

  <%= render @appointment %>

  <div>
    <% if doctor_signed_in? %>
      <%= link_to "Add recommendation", edit_appointment_path(@appointment) %> |
    <% end %>
    <%= link_to "Back to appointments", appointments_path %>

    <%= button_to "Destroy this appointment", @appointment, method: :delete %>
  </div>
</div>